#pragma once

namespace types {
    enum class Interaction {
        AcceptCompletion,
        CancelCompletion,
        DeleteInput,
        EnterInput,
        MouseClick,
        Navigate,
        NormalInput,
        Paste,
        Save,
        Undo,
    };
}
