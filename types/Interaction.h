#pragma once

namespace types {
    enum class Interaction {
        AcceptCompletion,
        CancelCompletion,
        DeleteInput,
        EnterInput,
        Navigate,
        NormalInput,
        Paste,
        Save,
        Undo,
    };
}
